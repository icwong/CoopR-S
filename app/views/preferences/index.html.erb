<p id="notice"><%= notice %></p>

  <div class="table-title">
        <h3>Listing Preferences</h3>
    </div>

<table class="table-list-users">
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @preferences.each do |preference| %>
      <tr>
        <td><%= preference.name %></td>
        <td><%= link_to 'Show', preference %></td>
        <td><%= link_to 'Edit', edit_preference_path(preference) %></td>
        <td><%= link_to 'Destroy', preference, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '<button class="button" type="button">New Preference</button>'.html_safe, new_preference_path %>
<button class="button" type="button" onclick="lastUrl()">Back</a>